<div class="mb-2">
    <div class="flex items-center space-x-3">
        <input
            type="checkbox"
            name="{{ $name }}"
            class="h-5 w-5"
        >
        <label for="{{ $name }}" class="text-gray-900 font-medium">{{ $label }}</label>
    </div>
</div>